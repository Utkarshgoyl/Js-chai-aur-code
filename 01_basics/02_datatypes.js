"use strict" // treat all JS Code as newer version

//alert( 3 + 3 ) we use it in browser but at now we are using nodejs

console.log( 3 
    + 
    3 ); // code readability should be high
            //     |
            //     |
            //    \ /

        console.log("Utkarsh");


let Name = "Utarsh"     // string datatype
let age = 19            // number datatype
let isLoggedIn = false  // boolean datatype
let state;              // Undefined

/*
                 DATATYPES  
            PRIMITIVE DATATYPES
    (meaning their values cannot be changed.)
    number => 2 to power 53
    bigint => used to store big values {like in stockmarket, facebook, reddit, etc.}
    string => "" anything in double quote is string
    boolean => true/false
    null => standalone value datatype
    undefined => the variable is defined but the value is not assigned till now
    symbol => unique id or value
*/

//         NON-PRIMITIVE (REFERENCE) DATATYPES 
// (meaning their properties and elements can be changed.)
// object 

    console.log(typeof undefined); // undefined
    console.log(typeof null);      // object